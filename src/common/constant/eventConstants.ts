export enum EBootstrapEvent {
  SERVER_STARTED = 'server.started',
}

export enum EUserEvent {
  USER_QUEST_CREATED = 'userQuest.created',
  USER_LOGIN = 'user.login',
  USER_LOGIN_NEW = 'user.login.new',
  USER_LOGIN_NEW_REWARD = 'user.login.new.reward',
}
